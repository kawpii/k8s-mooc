apiVersion: v1
kind: Service
metadata:
  namespace: project
  name: todo-broadcaster-svc
spec:
  type: ClusterIP
  selector:
    app: todo-broadcaster-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080