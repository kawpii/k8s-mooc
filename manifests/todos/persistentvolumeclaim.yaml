apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: images-claim
spec:
  storageClassName: standard-rwx
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi